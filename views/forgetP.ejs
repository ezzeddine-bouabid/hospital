<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        body {
            align-items: center;
            display: flex;
            justify-content: center;
            flex-direction: column;
            background: #f6f5f7;
            font-family: 'Montserrat', sans-serif;
            min-height: 100vh; /* Changed min-height to viewport height for full page coverage */
            margin: 0; /* Removed margin for cleaner appearance */
        }
        form {
            background: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            padding: 40px; /* Increased padding for better spacing */
            border-radius: 10px; /* Added border radius for rounded corners */
            box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.1); /* Added box shadow for depth */
            max-width: 400px; /* Added max-width for better responsiveness */
            width: 100%;
            text-align: center;
        }
        h2 {
            font-weight: bold;
            margin-bottom: 20px; /* Increased margin for better spacing */
            color: #333; /* Changed text color for better readability */
        }
        label {
            font-size: 14px; /* Increased font size for better visibility */
            color: #555; /* Changed text color for better readability */
            margin-bottom: 10px; /* Increased margin for better spacing */
        }
        input {
            background: #eee;
            padding: 12px 15px;
            margin: 8px 0;
            width: calc(100% - 30px); /* Adjusted width for better alignment */
            border-radius: 5px;
            border: none;
            outline: none;
        }
        button {
            color: #fff;
            background: green;
            font-size: 14px;
            font-weight: bold;
            padding: 12px 0;
            margin: 20px 0;
            border-radius: 20px;
            border: 1px solid green;
            outline: none;
            letter-spacing: 1px;
            text-transform: uppercase;
            transition: transform 0.2s ease-in-out;
            cursor: pointer;
            width: 100%; /* Adjusted width for full-width button */
            max-width: 200px; /* Added max-width to prevent overly wide buttons */
        }
        button:hover {
            background: #4CAF50; /* Darker green on hover for better contrast */
        }
        button:active {
            transform: scale(0.95); /* Slight scaling on click for visual feedback */
        }
        a {
            text-decoration: none; /* Removed underline from anchor */
            color: green; /* Changed link color to match button */
            margin-top: 10px; /* Added margin to separate button and link */
        }
        a:hover {
            text-decoration: underline; /* Underline on hover for visual feedback */
        }
    </style>
</head>
<body>
    <h2>Password Reset</h2>
    <form action="/forgetPassword" method="POST">
        <label for="email">Enter your email:</label>
        <input type="email" id="email" name="email" required>
        <button type="submit">Reset Password</button>
    </form>
    <a href="/logP">Return to Login</a>
</body>
</html>
